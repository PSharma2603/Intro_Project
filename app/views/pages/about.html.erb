<h1>About This Project</h1>
<p>This is an e-commerce platform built with Ruby on Rails, featuring product listings, orders, and vendors.</p>

<h2>Data Sources</h2>
<ul>
  <% @data_sources.each do |source| %>
    <li>
      <strong><%= source[:name] %></strong> - 
      <% if source[:url].start_with?("http") %>
        <a href="<%= source[:url] %>" target="_blank">View Source</a>
      <% else %>
        <%= source[:url] %>
      <% end %>
    </li>
  <% end %>
</ul>

<h2>Database Schema (ERD)</h2>
<p>The project follows the ERD structure below:</p>
<img src="<%= asset_path('erd.png') %>" alt="ERD Diagram" width="600">
